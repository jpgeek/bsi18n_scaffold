# frozen_string_literal: true

RSpec.describe Bsi18nScaffold do
  it "has a version number" do
    expect(Bsi18nScaffold::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
